# AMD ML Operators

Custom machine learning operators for AMD GPUs.

## Operators
- Fused attention (FlashAttention-style)
- Layer normalization
- Sparse matrix operations
- Custom activation functions
- Quantized linear layers
